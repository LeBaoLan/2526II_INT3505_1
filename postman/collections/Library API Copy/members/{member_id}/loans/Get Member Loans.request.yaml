$kind: http-request
url: "{{baseUrl}}/members/:member_id/loans?returned=false&overdue=false&sort_by\
  =borrowed_at&order=desc"
method: GET
headers:
  Accept: application/json
queryParams:
  returned: "false"
  overdue: "false"
  sort_by: borrowed_at
  order: desc
pathVariables:
  member_id: string
examples: ./.resources/Get Member Loans.resources/examples
order: 1000
