$kind: http-example
request:
  url: "{{baseUrl}}/members/:member_id/loans?returned=true&overdue=true&sort_by=b\
    orrowed_at&order=desc"
  method: GET
  headers:
    Accept: application/json
  queryParams:
    returned: "true"
    overdue: "true"
    sort_by: borrowed_at
    order: desc
  pathVariables:
    member_id: string
response:
  statusCode: 422
  statusText: Unprocessable Entity (WebDAV) (RFC 4918)
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "detail": [
          {
            "loc": [
              "string",
              1879
            ],
            "msg": "string",
            "type": "string"
          },
          {
            "loc": [
              4909,
              "string"
            ],
            "msg": "string",
            "type": "string"
          }
        ]
      }
order: 2000
